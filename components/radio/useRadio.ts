// useRadio — hook para consumir el contexto del RadioProvider
// Uso: const { isPlaying, play, pause, volume, setVolume, toggleMute, config } = useRadio()

export { useRadioContext as useRadio } from './RadioProvider'
